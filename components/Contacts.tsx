import React from "react";

const Contacts = () => {
  return (
    <section
      id="contacts"
      className="w-full h-screen px-40 bg-theme1-accent2 snap-start"
    >
      Contacts
    </section>
  );
};

export default Contacts;
